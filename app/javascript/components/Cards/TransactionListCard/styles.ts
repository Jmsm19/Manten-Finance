import { makeStyles } from '@material-ui/styles';

const useTransactionCardStyles = makeStyles({
  card: {
    overflowY: 'auto',
  },
  cardTitle: {
    paddingLeft: 16,
    paddingTop: 16,
  },
});

export default useTransactionCardStyles;
